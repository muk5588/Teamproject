<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="login.dao.SocialDao">
	
	<select id="getSosid" resultType="String" parameterType="String">
		select SUSERNO from usertb_test
		<where>
			and SUSERNO = #{id }
		</where>
	</select>

	<select id="socialLogin" resultType="UserDTO" parameterType="String">
		select name, nickname FROM usertb_test
		<where>
			and suserno = #{suserno }
		</where>
	</select>
	<insert id="socialJoin" parameterType="UserDTO">
		INSERT INTO usertb_test(userno, name, userid, userpw,  email, gender, postcode, address, detailAddress, extraAddress, phone, nickname,suserno)
		VALUES (user_seq_test.nextval, #{name}, #{userid},#{userpw}, #{email}, #{gender}, #{postcode}, #{address}, #{detailAddress}, #{extraAddress}, #{phone},  #{nickname}, #{suserno})
	</insert>
</mapper>














