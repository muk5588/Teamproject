<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user.dao.UserDao">
	<select id="userDetail" resultType="UserDTO">
		SELECT * FROM usertb_test WHERE userid=#{userid }
	</select>

	<select id="userList" resultType="UserDTO">
		SELECT * FROM usertb_test order by userno
	</select>

	<insert id="userInsert" parameterType="UserDTO">
		INSERT INTO usertb_test(userno, name, userid, userpw, email, gender, postcode, address, detailAddress, extraAddress, phone)
		VALUES (user_seq_test.nextval, #{name}, #{userid}, #{userpw}, #{email}, #{gender}, #{postcode}, #{address}, #{detailAddress}, #{extraAddress}, #{phone})
	</insert>

	<update id="userUpdate" parameterType="UserDTO">
		UPDATE usertb_test SET name = #{name},
		                  userpw = #{userpw},
		                  email=#{email},
		                  gender=#{gender},
		                  postcode=#{postcode},
		                  address =#{address},
		                  detailAddress = #{detailAddress},
		                  extraAddress = #{extraAddress},
		                  phone = #{phone}
		WHERE userid=#{userid }
	</update>

	<delete id="userDelete" parameterType="int">
		DELETE FROM usertb_test WHERE userid=#{userid }
	</delete>
</mapper>

